# Lectoria BookManager Environment Configuration

# Application
NODE_ENV=production
PORT=3000

# Security - CRITICAL: Change these values in production!
JWT_SECRET=your-super-secret-jwt-key-change-me-to-something-very-secure

# Database
# SQLite database file is stored in ./data/bookmanager.db
# Make sure the data directory is persistent in production

# File Upload
# Upload directory: ./uploads (make persistent in production)
# Max file size: 50MB (configured in backend_server.js)

# CORS Settings (for production, specify your domain)
# FRONTEND_URL=https://yourdomain.com

# Optional: Database Backup
# BACKUP_INTERVAL=daily