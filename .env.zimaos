# ZimaOS Environment Configuration for Lectoria
# Copy this file to .env in your ZimaOS installation directory

# Production Environment
NODE_ENV=production
PORT=3000

# Security - JWT_SECRET (REQUIRED for current Docker image)
# Generate with: openssl rand -base64 64
JWT_SECRET=lectoria-zimaos-default-jwt-secret-2024-please-change-me-for-production

# Database Configuration - SQLite
DB_PATH=/app/data/lectoria.db

# File Upload Limits
MAX_FILE_SIZE=70485760    # 70MB for PDF/EPUB files
MAX_IMAGE_SIZE=5242880    # 5MB for cover images

# CORS Configuration
FRONTEND_URL=http://localhost:3000

# User/Group IDs for file permissions (ZimaOS/Linux)
# Find your IDs with: id
PUID=1000
PGID=1000

# Optional: Custom settings
# RATE_LIMIT_WINDOW_MS=900000     # 15 minutes
# RATE_LIMIT_MAX_REQUESTS=100     # Max requests per window